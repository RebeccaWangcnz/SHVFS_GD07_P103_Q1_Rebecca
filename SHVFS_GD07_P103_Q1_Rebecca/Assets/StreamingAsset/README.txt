1. The Update is called every frame; The FixedUpfate is called every fixed frame-rate frame. The LateUpdate is called after all update function have been called.
2. Any files placed in a folder called StreamingAssets in a Unity project will be copied verbatim to a particular folder on the target machine.
3. resource: Load thing at runtime.
4.  You can set the execution order by the project setting-->script execution order. 